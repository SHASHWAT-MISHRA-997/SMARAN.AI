# Copy to keystore.properties (git-ignored) to build a signed release.
# The real keystore is not in this repository by design.
storeFile=keystore/smaran-release.jks
storePassword=
keyAlias=smaran
keyPassword=
