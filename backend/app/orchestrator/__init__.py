"""Splitting one project across several models without them colliding.

Named `orchestrator` rather than `director` because `app.director` already
exists and turns a script into a video. The user-facing name for this is still
the Director.
"""
