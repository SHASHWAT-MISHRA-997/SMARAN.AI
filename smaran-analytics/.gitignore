# The ingest secret and the collected data never belong in version control.
ingest-key.txt
analytics.db
analytics.db-*
__pycache__/
dashboard-key.txt

# Deploy-time secrets, uploaded with the deploy, never committed.
hosted/keys.json
