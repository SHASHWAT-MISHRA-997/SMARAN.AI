# The ingest secret and the collected data never belong in version control.
ingest-key.txt
analytics.db
analytics.db-*
__pycache__/
dashboard-key.txt

# Raw credentials stay local; only their verification hashes are deployed.
hosted/keys.json
hosted/key-hashes.json

# Netlify CLI scratch state (just the site id, but build noise).
.netlify/
